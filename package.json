{
  "name": "clear",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:ProductSearch": "npx cypress run --spec cypress/e2e/tests/searchProductTest.cy.js",
    "test:CheckBox": "npx cypress run --spec cypress/e2e/tests/checkBoxRadioButton.cy.js",
    "test:testFolder": "npx cypress run --spec cypress/e2e/tests",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.9.0",
    "cypress-mochawesome-reporter": "^3.8.2",
    "cypress-xpath": "^2.0.1"
  }
}

